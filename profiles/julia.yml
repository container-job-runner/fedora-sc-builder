build:
  args:
    # ---- languages -----------------------------------------------------------
    LANG_JULIA: "TRUE"
    # ---- libraries -----------------------------------------------------------
    LIB_MATPLOTLIB : "TRUE"
    # ---- dev enviroment ------------------------------------------------------
    DEV_CLI: "TRUE"
