build:
  args:
    # ---- languages -----------------------------------------------------------
    LANG_PYTHON: "TRUE"
    # ---- libraries -----------------------------------------------------------
    LIB_MATPLOTLIB : "TRUE"
    # ---- dev enviroment ------------------------------------------------------
    DEV_CLI: "TRUE"
