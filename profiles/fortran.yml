build:
  args:
    # ---- languages -----------------------------------------------------------
    LANG_FORTRAN: "TRUE"
    # ---- libraries -----------------------------------------------------------
    LIB_LINALG : "TRUE"
    LIB_OPENMPI: "TRUE"
    # ---- dev enviroment ------------------------------------------------------
    DEV_CLI: "TRUE"
