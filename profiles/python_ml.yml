build:
  args:
    # ---- languages -----------------------------------------------------------
    LANG_PYTHON3: "TRUE"
    # ---- libraries -----------------------------------------------------------
    LIB_PYTORCH : "TRUE"
    LIB_JAX : "TRUE"
    # ---- dev enviroment ------------------------------------------------------
    DEV_CLI: "TRUE"
